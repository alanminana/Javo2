@model Javo2.ViewModels.Operaciones.Ventas.VentaFormViewModel
@{
    ViewData["Title"] = Model.VentaID == 0 ? "Crear Venta" : "Editar Venta";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="container-fluid py-3">
    <h1 class="h2 mb-4">@ViewData["Title"]</h1>

    <div class="card shadow-sm bg-dark text-light">
        <div class="card-body">
            <form asp-action="@(Model.VentaID == 0 ? "Create" : "Edit")" method="post" id="ventaForm" novalidate>
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                @if (Model.VentaID != 0)
                {
                    <input type="hidden" asp-for="VentaID" />
                }

                <!-- Datos de la Venta -->
                <fieldset class="mb-4">
                    <legend class="text-light">Datos de la Venta</legend>
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label asp-for="FechaVenta" class="form-label text-light">Fecha</label>
                            <input asp-for="FechaVenta" type="date" class="form-control bg-secondary text-light" />
                            <span asp-validation-for="FechaVenta" class="invalid-feedback d-block"></span>
                        </div>
                        <div class="col-md-6">
                            <label asp-for="NumeroFactura" class="form-label text-light">Factura</label>
                            <input asp-for="NumeroFactura" class="form-control bg-secondary text-light" readonly />
                            <span asp-validation-for="NumeroFactura" class="invalid-feedback d-block"></span>
                        </div>
                    </div>
                </fieldset>

                <!-- Datos del Cliente -->
                <fieldset class="mb-4">
                    <legend class="text-light">Datos del Cliente</legend>
                    <div class="row g-3">
                        <div class="col-md-4">
                            <label for="DniCliente" class="form-label text-light">DNI</label>
                            <div class="input-group">
                                <input type="number" id="DniCliente" class="form-control bg-secondary text-light" placeholder="Ingrese DNI" aria-label="DNI Cliente" />
                                <button type="button" id="buscarCliente" class="btn btn-primary" aria-label="Buscar Cliente">
                                    <i class="bi bi-search"></i>
                                </button>
                            </div>
                        </div>
                        <div class="col-md-8">
                            <label asp-for="NombreCliente" class="form-label text-light">Cliente</label>
                            <input asp-for="NombreCliente" class="form-control bg-secondary text-light" readonly />
                            <span asp-validation-for="NombreCliente" class="invalid-feedback d-block"></span>
                        </div>
                    </div>
                    <div class="row g-3 mt-3">
                        <div class="col-md-4">
                            <label asp-for="TelefonoCliente" class="form-label text-light">Teléfono</label>
                            <input asp-for="TelefonoCliente" class="form-control bg-secondary text-light" readonly />
                        </div>
                        <div class="col-md-4">
                            <label asp-for="DomicilioCliente" class="form-label text-light">Domicilio</label>
                            <input asp-for="DomicilioCliente" class="form-control bg-secondary text-light" readonly />
                        </div>
                        <div class="col-md-4">
                            <label asp-for="LocalidadCliente" class="form-label text-light">Localidad</label>
                            <input asp-for="LocalidadCliente" class="form-control bg-secondary text-light" readonly />
                        </div>
                        <div class="col-md-4">
                            <label asp-for="CelularCliente" class="form-label text-light">Celular</label>
                            <input asp-for="CelularCliente" class="form-control bg-secondary text-light" readonly />
                        </div>
                        <div class="col-md-4">
                            <label asp-for="LimiteCreditoCliente" class="form-label text-light">Límite Crédito</label>
                            <input asp-for="LimiteCreditoCliente" class="form-control bg-secondary text-light" readonly />
                        </div>
                        <div class="col-md-4">
                            <label asp-for="SaldoCliente" class="form-label text-light">Saldo</label>
                            <input asp-for="SaldoCliente" class="form-control bg-secondary text-light" readonly />
                        </div>
                        <div class="col-md-4">
                            <label asp-for="SaldoDisponibleCliente" class="form-label text-light">Saldo Disponible</label>
                            <input asp-for="SaldoDisponibleCliente" class="form-control bg-secondary text-light" readonly />
                        </div>
                    </div>
                </fieldset>

                <!-- Forma de Pago -->
                <fieldset class="mb-4">
                    <legend class="text-light">Forma de Pago</legend>
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label asp-for="FormaPagoID" class="form-label text-light">Método</label>
                            <select asp-for="FormaPagoID" class="form-select bg-secondary text-light" id="FormaPagoID">
                                <option value="">-- Seleccione --</option>
                                @foreach (var fp in Model.FormasPago)
                                {
                                    <option value="@fp.Value">@fp.Text</option>
                                }
                            </select>
                            <span asp-validation-for="FormaPagoID" class="invalid-feedback d-block"></span>
                        </div>
                        <div class="col-md-6" id="pagoAdicionalContainer"></div>
                    </div>
                </fieldset>

                <!-- Anticipo y Detalles de Pago -->
                <fieldset class="mb-4">
                    <legend class="text-light">Anticipo / Cheques / Crédito</legend>
                    <div class="row g-3">
                        <div class="col-md-3">
                            <label asp-for="AdelantoDinero" class="form-label text-light">Anticipo</label>
                            <input asp-for="AdelantoDinero" type="number" min="0" step="0.01" class="form-control bg-secondary text-light" />
                            <span asp-validation-for="AdelantoDinero" class="invalid-feedback d-block"></span>
                        </div>
                        <div class="col-md-3">
                            <label asp-for="DineroContado" class="form-label text-light">Contado</label>
                            <input asp-for="DineroContado" type="number" min="0" step="0.01" class="form-control bg-secondary text-light" />
                            <span asp-validation-for="DineroContado" class="invalid-feedback d-block"></span>
                        </div>
                        <div class="col-md-3">
                            <label asp-for="MontoCheque" class="form-label text-light">Monto Cheque</label>
                            <input asp-for="MontoCheque" type="number" min="0" step="0.01" class="form-control bg-secondary text-light" />
                            <span asp-validation-for="MontoCheque" class="invalid-feedback d-block"></span>
                        </div>
                        <div class="col-md-3">
                            <label asp-for="NumeroCheque" class="form-label text-light">N° Cheque</label>
                            <input asp-for="NumeroCheque" class="form-control bg-secondary text-light" />
                            <span asp-validation-for="NumeroCheque" class="invalid-feedback d-block"></span>
                        </div>
                    </div>
                </fieldset>

                <!-- Tabla de Productos -->
                <fieldset class="mb-4">
                    <legend class="text-light">Detalle de Productos</legend>
                    <div class="table-responsive">
                        <table class="table table-hover table-dark table-striped mb-0" id="productosTable">
                            <thead>
                                <tr>
                                    <th>Código</th>
                                    <th>Producto</th>
                                    <th>Cant.</th>
                                    <th>Precio Unit.</th>
                                    <th>Subtotal</th>
                                    <th class="text-center">Acción</th>
                                </tr>
                            </thead>
                            <tbody>
                                @for (int i = 0; i < Model.ProductosPresupuesto.Count; i++)
                                {
                                    var p = Model.ProductosPresupuesto[i];
                                    <tr data-index="@i">
                                        <td>
                                            <input type="hidden" name="ProductosPresupuesto[@i].ProductoID" value="@p.ProductoID" />
                                            @p.CodigoBarra
                                        </td>
                                        <td>@p.NombreProducto</td>
                                        <td><input type="number" name="ProductosPresupuesto[@i].Cantidad" value="@p.Cantidad" min="1" class="form-control bg-secondary text-light cantidad" /></td>
                                        <td>@p.PrecioUnitario.ToString("C")</td>
                                        <td><span class="subtotal">@(p.Cantidad * p.PrecioUnitario).ToString("C")</span></td>
                                        <td class="text-center">
                                            <button type="button" class="btn btn-sm btn-outline-danger eliminar-producto" aria-label="Eliminar producto">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </fieldset>

                <!-- Totales -->
                <div class="text-end mb-4">
                    <h4 class="mb-1">Total Productos: <span id="totalProductos">@Model.TotalProductos</span></h4>
                    <h4>Total Venta: <span id="totalVenta">@Model.PrecioTotal.ToString("C")</span></h4>
                </div>

                <!-- Botones de Acción -->
                <div class="d-flex justify-content-end gap-2">
                    <button type="submit" name="Finalizar" value="false" class="btn btn-outline-light">
                        <i class="bi bi-save me-1"></i>Guardar Borrador
                    </button>
                    <button type="submit" name="Finalizar" value="true" class="btn btn-success">
                        <i class="bi bi-cash-coin me-1"></i>Finalizar Venta
                    </button>
                </div>
            </form>
        </div>
    </div>
</section>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
    <script defer>
        $(function() {
            function togglePagoFields() {
                const id = +$('#FormaPagoID').val();
                const containers = ['#bancoContainer','#tipoTarjetaContainer','#cuotasContainer','#entidadElectronicaContainer','#planFinanciamientoContainer'];
                containers.forEach(c=>$(c).addClass('d-none'));
                if (id===2) $('#bancoContainer,#tipoTarjetaContainer,#cuotasContainer').removeClass('d-none');
                if (id===5) $('#entidadElectronicaContainer').removeClass('d-none');
                if (id===6) $('#planFinanciamientoContainer').removeClass('d-none');
            }
            function updateTotals() {
                let totalP=0,totalV=0;
                $('#productosTable tbody tr').each(function(){
                    const qty=+$(this).find('.cantidad').val()||0;
                    const price=+$(this).find('input[name$=".PrecioUnitario"]').val()||0;
                    const sub = qty*price;
                    totalP+=qty; totalV+=sub;
                    $(this).find('.subtotal').text(new Intl.NumberFormat('es-AR',{style:'currency',currency:'ARS'}).format(sub));
                });
                $('#totalProductos').text(totalP);
                $('#totalVenta').text(new Intl.NumberFormat('es-AR',{style:'currency',currency:'ARS'}).format(totalV));
            }
            $('#FormaPagoID').change(togglePagoFields);
            $('#buscarCliente').click(e=>{/* AJAX buscar cliente */});
            $('#buscarProducto').click(e=>{/* AJAX buscar producto */});
            $(document).on('change','.cantidad',updateTotals);
            $(document).on('click','.eliminar-producto',function(){
                $(this).closest('tr').remove(); updateTotals();
            });
            togglePagoFields(); updateTotals();
        });
    </script>
}
