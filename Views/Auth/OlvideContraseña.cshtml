@model Javo2.ViewModels.Authentication.OlvideContraseñaViewModel
@{
    ViewData["Title"] = "Recuperar Contraseña";
    Layout = "~/Views/Shared/_LayoutLogin.cshtml";
}

<div class="login-container">
    <div class="text-center mb-4">
        <img src="~/img/logo.png" alt="Javo2 Logo" class="app-logo" onerror="this.src='/img/placeholder-logo.png'; this.onerror=null;">
        <h1 class="h3 mb-3 fw-normal text-light">Recuperación de Contraseña</h1>
    </div>

    <div class="card login-card">
        <div class="card-header py-3">
            <h2 class="card-title h5 mb-0 text-center">Solicitar Restablecimiento</h2>
        </div>
        <div class="card-body">
            @if (ViewBag.ConfirmacionEnviada == true)
            {
                <div class="alert alert-success">
                    <h4 class="alert-heading"><i class="bi bi-envelope-check me-2"></i>Instrucciones Enviadas</h4>
                    <p>Si el usuario o correo electrónico existe en nuestro sistema, recibirá un correo con las instrucciones para restablecer su contraseña.</p>
                    <p>Por favor, revise su bandeja de entrada y, si es necesario, su carpeta de spam.</p>
                    <hr>
                    <p class="mb-0">Si no recibe el correo en los próximos minutos, puede intentar nuevamente.</p>
                </div>
                <div class="d-grid gap-2 mt-4">
                    <a asp-controller="Auth" asp-action="Login" class="btn btn-primary">
                        <i class="bi bi-arrow-left-circle me-1"></i> Volver al Inicio de Sesión
                    </a>
                </div>
            }
            else
            {
                <p class="text-center text-muted mb-4">
                    Ingrese su nombre de usuario o correo electrónico asociado a su cuenta y le enviaremos instrucciones para restablecer su contraseña.
                </p>
                <form asp-action="OlvideContraseña" method="post">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                    <div class="mb-3">
                        <label asp-for="NombreUsuario" class="form-label">Nombre de usuario</label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="bi bi-person"></i></span>
                            <input asp-for="NombreUsuario" class="form-control" placeholder="Ingrese su nombre de usuario" />
                        </div>
                        <span asp-validation-for="NombreUsuario" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Email" class="form-label">Email</label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="bi bi-envelope"></i></span>
                            <input asp-for="Email" class="form-control" placeholder="Ingrese su correo electrónico" />
                        </div>
                        <span asp-validation-for="Email" class="text-danger"></span>
                        <div class="form-text text-muted">Ingrese su nombre de usuario o correo electrónico, no es necesario completar ambos campos.</div>
                    </div>

                    <div class="d-grid gap-2 mt-4">
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-envelope me-1"></i> Enviar Instrucciones
                        </button>
                        <a asp-controller="Auth" asp-action="Login" class="btn btn-outline-secondary">
                            <i class="bi bi-arrow-left me-1"></i> Volver al Login
                        </a>
                    </div>
                </form>
            }
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
        <script src="~/js/Auth/OlvideContraseña.js"></script>



    }
}