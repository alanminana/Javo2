@{
    ViewData["Title"] = "Token Inválido";
    Layout = "~/Views/Shared/_LayoutLogin.cshtml";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-lg border-0 rounded-lg mt-5 bg-dark text-light">
            <div class="card-header bg-danger text-white">
                <h3 class="text-center font-weight-light my-4">
                    <i class="bi bi-exclamation-triangle-fill me-2"></i>Token Inválido
                </h3>
            </div>
            <div class="card-body">
                <div class="text-center mb-4">
                    <i class="bi bi-x-circle text-danger" style="font-size: 4rem;"></i>
                </div>

                <h4 class="text-center mb-3">El enlace de restablecimiento no es válido</h4>

                <p class="text-center">
                    El enlace que ha utilizado para restablecer su contraseña no es válido o ha expirado.
                </p>

                <p class="text-center">
                    Los enlaces de restablecimiento de contraseña son válidos solo por 24 horas desde su generación.
                </p>

                <div class="d-grid gap-2 mt-4">
                    <a asp-controller="ResetPassword" asp-action="OlvideContraseña" class="btn btn-primary">
                        <i class="bi bi-arrow-clockwise me-1"></i> Solicitar Nuevo Enlace
                    </a>
                    <a asp-controller="Auth" asp-action="Login" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left me-1"></i> Volver al Inicio de Sesión
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>